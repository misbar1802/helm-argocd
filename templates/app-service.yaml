apiVersion: v1
kind: Service
metadata:
  name: flask-app-svc
  namespace: default
  labels:
    app: flask-app
spec:
  selector:
    app: flask-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000
